services:
  # ===== PostgreSQL Database =====
  postgres:
    image: postgres:15-alpine
    container_name: elion-mdm-postgres
    environment:
      # Lidos do .env raiz
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD?A senha do banco precisa ser configurada no .env}
      POSTGRES_DB: ${DB_NAME:-mdm_project}
    ports:
      # Porta 5432 mapeada para o Host (Permite seu pgAdmin local acessar via localhost:5432)
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - elion-network
    # IMPORTANTE: Healthcheck garante que o banco ligou antes do backend subir
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-mdm_project}" ]
      interval: 5s
      timeout: 5s
      retries: 5

  # ===== Backend FastAPI Server =====
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: elion-mdm-backend
    environment:
      # IMPORTANTE: Dentro da rede Docker, o backend enxerga o banco pelo nome do servico 'postgres'
      # NOTA: Usar DATABASE_URL_DOCKER que sobrepõe a DATABASE_URL para forçar conexão "postgres:5432"
      DATABASE_URL: postgresql+asyncpg://${DB_USER:-postgres}:${DB_PASSWORD}@postgres:5432/${DB_NAME:-mdm_project}
      SECRET_KEY: ${SECRET_KEY?Missing SECRET_KEY}
      BOOTSTRAP_SECRET: ${BOOTSTRAP_SECRET?Missing BOOTSTRAP_SECRET}
      DEFAULT_ADMIN_PASSWORD: ${DEFAULT_ADMIN_PASSWORD?Missing DEFAULT_ADMIN_PASSWORD}
      DEBUG: ${DEBUG:-True}
      ENVIRONMENT: ${ENVIRONMENT:-development}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS}
    # ⚠️ CRÍTICO (P1.2): Backend NÃO exposto em 8000 para o host
    # Apenas Nginx (reverse proxy) comunica com backend internamente
    # ports:
    #   - "8000:8000"  ← REMOVIDO POR SEGURANÇA
    depends_on:
      postgres:
        condition: service_healthy # Só sobe quando o Postgres diz que está pronto
    volumes:
      - ./backend:/app/backend:ro
    networks:
      - elion-network
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload

  # ===== Frontend Vite + Node =====
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: elion-mdm-frontend
    environment:
      # VITE_API_BASE_URL: URL usada DENTRO do container para chamar o backend
      # Dentro do Docker, usar nome do serviço (nginx) em vez de localhost
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://nginx/api}
    ports:
      - "3000:3000"
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
      - /app/node_modules
    networks:
      - elion-network
    command: npm run dev -- --host 0.0.0.0 --port 3000

  # ===== Nginx Reverse Proxy (Opcional - útil na Fase de Produção) =====
  nginx:
    image: nginx:alpine
    container_name: elion-mdm-nginx
    ports:
      - "80:80"
      - "8080:80"
      # - "443:443"  # Comentada para desenvolvimento (usar HTTP)
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - backend
      - frontend
    networks:
      - elion-network

networks:
  elion-network:
    driver: bridge

volumes:
  # Volume para reter os dados do banco mesmo que os containers sejam destruídos
  postgres_data:
    driver: local
